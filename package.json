{
  "name": "soul-miners-eden",
  "version": "1.0.0",
  "description": "Soul Miner's Eden - Astro frontend",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "parse:products": "node scripts/parse-products.js",
    "migrate:images": "node scripts/migrate-images.js",
    "migrate:product-images": "node scripts/migrate-product-images.js"
  },
  "devDependencies": {
    "cloudinary": "^2.8.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
